@model AdminRegisterVm


@{
    ViewBag.Title = "Admin | Create Account";
}

<form class="form" method="post" enctype="multipart/form-data" data-ajax="false">
    <div asp-validation-summary="ModelOnly"></div>

    <label asp-for="Name"></label>
    <input asp-for="Name" autofocus>
    <span asp-validation-for="Name"></span>

    <label asp-for="Email"></label>
    <input asp-for="Email">
    <span asp-validation-for="Email"></span>

    <label asp-for="Phone"></label>
    <input asp-for="Phone">
    <span asp-validation-for="Phone"></span>

    <label asp-for="Password"></label>
    <input asp-for="Password">
    <span asp-validation-for="Password"></span>

    <label asp-for="ConfirmPassword"></label>
    <input asp-for="ConfirmPassword">
    <span asp-validation-for="ConfirmPassword"></span>

    <label asp-for="Photo"></label>
    <label class="upload">
        <small>Select Photo...</small>
        <input asp-for="Photo" accept="image/jpeg,image/png" hidden>
        <img src="/photos/adminprofile/default.jpg" alt="Preview" id="adminPhotoPreview">
    </label>
    <span asp-validation-for="Photo"></span>

    <section>
        <input type="submit" value="Register" class="btn btn-primary" />
        <input type="reset" value="Reset" class="btn btn-secondary" />
    </section>
</form>

@section foot {
    <script>
        $.validator.setDefaults({ ignore: '' });

        document.querySelector('input[name="Photo"]').addEventListener('change', function (e) {
            const file = e.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function (event) {
                    document.querySelector('.upload img').src = event.target.result;
                }
                reader.readAsDataURL(file);
            }
        });
    </script>
}