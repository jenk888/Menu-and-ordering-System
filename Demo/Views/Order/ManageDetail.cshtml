@model Order

@{
    ViewBag.Title = $"Order #{Model.Id}";
    ViewBag.IsAdmin = true;
}

<div class="container my-4">
    <a asp-action="Manage" class="btn btn-outline-secondary btn-sm mb-3">&larr; Back to Orders</a>

    <partial name="_OrderDetailBody" model="Model" />
</div>
